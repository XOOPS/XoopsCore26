build:
  nodes:
    analysis:
      tests:
        override:
          - php-scrutinizer-run

filter:
  excluded_paths:
    - 'tests/'
    - 'extras/'
    - 'console/'
    - 'upgrade/'
    - 'docs/'
  dependency_paths:
    - 'xoops_lib/vendor/'

tools:
  php_analyzer: true
